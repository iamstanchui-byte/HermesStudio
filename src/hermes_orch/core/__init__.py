# coding: utf-8
"""Core domain logic (per REVIEW.md §1, §3, §4).

Modules (filled in as we build):
- dispatcher.py    — task dispatch with role → agent resolution, §1.2 protocol
- supervisor.py    — supervisor session lifecycle, §3.3
- session.py       — Hermes session management, §2.2
- plan_poller.py   — plan.md 5s polling, §4.4
"""
